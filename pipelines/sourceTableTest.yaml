description: |
  Test out if this runs using the middleware
tasks:
  sourceTableVisit:
    class: lsst.analysis.tools.pipelineTasks.sourceTableVisitAnalysis.SourceTableVisitAnalysisTask
    config:
      bands: ['r', 'i']
